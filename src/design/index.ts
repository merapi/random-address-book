export enum Spacing {
  None = 0,
  Tiny = 4,
  Small = 8,
  Base = 16,
  Medium = 24,
  Large = 32,
  Huge = 64,
}

export enum Size {
  CardGap = Spacing.Large,
}

export enum FontSize {
  Smaller = 14,
  Base = 16,
  Bigger = 20,
}

export enum Color {
  Primary = '#0C9E88',
}
